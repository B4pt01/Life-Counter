import React from 'react';
import { Text, View, Button } from 'react-native';
// import { Reset } from '../LifeCount/LifeCount';

export function Nav({ reset }) {
	return (
		<>
			<Text style={{ fontSize: 25, textAlign: 'center', backgroundColor: 'grey', color: 'white' }}>hello</Text>
			{/* <Reset /> */}
		</>
	);
}
